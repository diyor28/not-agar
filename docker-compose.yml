version: '3.8'

services:
  back:
    build: ./back
    restart: always
    networks:
      - traefik

  front:
    build:
      context: ./front
      target: production
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.notagar.rule=Host(`notagar.diyor28.com`)"
      - "traefik.http.routers.notagar.tls=true" # secure using TLS.
    networks:
      - traefik

networks:
  traefik:
    name: traefik
    external: true
